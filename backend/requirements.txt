# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.0
sse-starlette==2.1.3
python-multipart==0.0.17

# TTS - this pulls in torch, transformers, nemo-toolkit, etc.
# nemo-toolkit requires numpy<2.0.0, so we don't override it
kani-tts==0.0.4
